// Bandicoot release version, distinct from the upstream Coot VERSION
// macro (which autoconf substitutes from configure.ac's AC_INIT and
// pins to the Coot snapshot we forked from). Bump this on each
// Bandicoot release. Used by the title bar and any --version / about
// dialog output that wants to distinguish Bandicoot's release from
// Coot's underlying version.

#ifndef BANDICOOT_VERSION_H
#define BANDICOOT_VERSION_H

#define BANDICOOT_VERSION "0.1.0.3"

#endif
